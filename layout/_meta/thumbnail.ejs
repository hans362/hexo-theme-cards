<% if (theme.meta.thumbnail.enable === true && (post.thumbnail || theme.meta.thumbnail.default)) { %>
    <% if (theme.lazyload && theme.lazyload.enable === true) { %>
        <img alt="Cover Image" class="lazy" src="<%= post.thumbnail || theme.meta.thumbnail.default %>" data-srcset="<%= post.thumbnail || theme.meta.thumbnail.default %>">
    <% } else { %>
        <img alt="Cover Image" src="<%= post.thumbnail || theme.meta.thumbnail.default %>">
    <% } %>
<% } %>